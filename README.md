# Smart Invoicing & Pricing — Agentic + Oracle + Reporting (v5)

**No `assert { type: "json" }` anywhere.** All JSON is read via `fs/promises` for compatibility.

## Run
```bash
# Server
cd server
npm i
npm run dev   # http://localhost:8787

# UI
cd ../ui
npm i
npm run dev   # http://localhost:5173
```
